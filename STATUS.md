# GeoAR Platform — Status (12 July 2026, updated after orchestration-prompt session)

One page: what is **real**, what is **simulated**, what is **architecture-only**
— per component, so tomorrow's status is unambiguous. Meeting deadline: 24 July.

## Prototype A — current XREAL hardware (`prototype-a-xreal-now/`)

| Component | Status | Detail |
|---|---|---|
| Model optimisation pipeline | **REAL** (placeholder input) | Runs end-to-end: cleanup → quadric decimation → LOD 100k/25k/5k GLBs → quality report, all checks passing. Input is a *procedural placeholder* chapel mesh — the real Oystermouth LiDAR asset drops into the same code path. Texture bake is a declared stub (needs the Blender job). |
| Fusion decision logic | **REAL** (mocked sensors) | Full spec decision table implemented and tested (31 pytest cases): fused lock, visual-only, anchor fallback + rescan flag, re-localization, explicit no-content. GPS accuracy and visual confidence are **mocked scenario feeds** — no live device session has run. |
| Field-test logging | **REAL** | Emits sessions conforming to the shared schema v1; every non-device session is tagged `synthetic`/`replay` at the row level. |
| Unity/XREAL scene | **CODE-COMPLETE, UNVERIFIED** | Anchor persistence, LOD renderer, fusion controller (mirrors the tested Python core), scene builder. Compiles against a provider interface without the SDK; the NRSDK adapter is unwritten pending SDK version pinning. **Not yet opened in Unity, not on device.** This is the critical path to 24 July. |

## Shared infrastructure (`shared-infrastructure/`)

| Component | Status | Detail |
|---|---|---|
| Backend API | **REAL** | FastAPI: site registry, anchors (live-overwrite approval gate), schema-validated log ingest, versioned thresholds, validated scan upload, model delivery. 23 tests. Live smoke-tested. SQLite dev DB; Postgres is a connection-string swap. |
| Processing job queue | **REAL** | Celery + Redis: upload auto-enqueues cleanup → LODs → bake (stub) → target (stub) with per-step status; failure retries once with a conservative profile then reports error + capture recommendation. Verified live through a real worker. |
| Deploys | **REAL (staging only)** | Versioned zip bundles (LODs + manifest) with explicit-confirmation gate for live-anchor sites. "Delivered-to-device" is not claimed — device pull is a Prototype A on-device task. |
| Field-test data | **SYNTHETIC** | 25 seeded sessions generated by replaying the fusion mock scenarios; tagged `source=synthetic` in every row and labelled in the UI. **Zero field sessions exist.** |
| Self-learning loop | **REAL v0, synthetic data** | Rule-based (explicitly not ML). Ran one genuine cycle tonight: LOOSEN `visual_confidence_min` 0.70 → 0.65 (threshold v1 → v2), before/after report committed. Second run correctly holds. 9 rule tests. |
| Web app | **REAL (all 7 pages)** | Upload, Processing status, Site registry, Site detail + 3D preview, Deploy, Field-test monitor (provenance filter + trend charts), Self-learning panel (before/after + run-cycle button). Production build passes; every page smoke-tested live and screenshot-reviewed. Operator token never reaches the browser. |
| Deployment (Docker) | **CONFIG-VALIDATED** | docker-compose (postgres + redis + api + worker + web) + Dockerfiles; `docker compose config` passes. Full `up --build` **[Needs verification]** — this container's network policy blocks Docker Hub pulls. |

## Prototype B — XREAL Aura / Android XR (`prototype-b-aura-future/`)

| Component | Status | Detail |
|---|---|---|
| Everything | **ARCHITECTURE-ONLY** | Hardware ships Fall 2026; nothing is built or claimable as tested. Delivered: layer-separation, anchor-lifecycle and fallback diagrams grounded in Prototype A's real interfaces; Niantic Spatial vs MultiSet comparison with recommendation (Niantic primary, MultiSet qualified in parallel); migration plan identifying exactly one cross-prototype refactor (`IVisualLocalizer` → `IGeospatialLocalizer`); stub Unity scaffold carrying that seam (`IGeospatialLocalizer`, `ArCoreAnchorProvider` behind `ANDROID_XR_PRESENT`, VPS client stub) — package versions and all SDK semantics `[Needs verification]`. |

## Before 24 July (in priority order)

1. **Unity verification ladder** (`prototype-a-xreal-now/unity/.../SceneSetup.md`):
   editor compile → play-mode sim → SDK import + NRSDK adapter → device build →
   field test at the site. Until step 5, "working demo" cannot be claimed.
2. Real Oystermouth asset through the pipeline (replaces placeholder; texture bake job).
3. Visual fine-lock provider decision for Prototype A (MultiSet trial vs custom matcher).
4. First real field sessions → `source=field` rows replace synthetic in the monitor.
5. `docker compose up --build` on the dev machine (blocked here by network policy).

## Test totals

63 automated tests passing (31 Prototype A · 23 backend · 9 self-learning),
plus live smoke: upload → Redis-brokered worker job → deploy bundle → learning
cycle via API, all 7 web pages rendered against the live backend and
screenshot-reviewed.
